<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FITGEM - Premium Sri Lankan Gemstone Marketplace</title>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600&family=Inter:wght@200;300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <style>
        :root { --platinum: #E5E4E2; --champagne: #F7E7CE; --rose-gold: #B76E79; --dark: #0a0a0a; --darker: #050505; --glass: rgba(255,255,255,0.03); --glass-border: rgba(255,255,255,0.08); }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Inter', sans-serif; background: var(--darker); color: var(--platinum); overflow-x: hidden; font-weight: 300; }
        h1, h2, h3, h4, h5, h6 { font-family: 'Cormorant Garamond', serif; font-weight: 300; letter-spacing: 0.02em; }
        .shimmer { background: linear-gradient(90deg, var(--platinum), var(--champagne), var(--rose-gold), var(--champagne), var(--platinum)); background-size: 200% auto; -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; animation: shimmer 3s linear infinite; }
        @keyframes shimmer { 0% { background-position: 200% center; } 100% { background-position: -200% center; } }
        #particle-canvas { position: fixed; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; z-index: 1; }
        .bg-grid { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background-image: linear-gradient(rgba(247,231,206,0.02) 1px, transparent 1px), linear-gradient(90deg, rgba(247,231,206,0.02) 1px, transparent 1px); background-size: 60px 60px; pointer-events: none; z-index: 0; }
        .glass { background: rgba(255,255,255,0.03); backdrop-filter: blur(30px); -webkit-backdrop-filter: blur(30px); border: 1px solid rgba(255,255,255,0.08); }
        .navbar { position: fixed; top: 0; width: 100%; z-index: 1000; padding: 20px 0; transition: all 0.3s ease; }
        .navbar.scrolled { background: rgba(5,5,5,0.9); backdrop-filter: blur(20px); padding: 15px 0; }
        .nav-container { max-width: 1400px; margin: 0 auto; padding: 0 40px; display: flex; justify-content: space-between; align-items: center; }
        .logo { font-family: 'Cormorant Garamond', serif; font-size: 28px; font-weight: 300; color: var(--champagne); text-decoration: none; letter-spacing: 3px; }
        .nav-links { display: flex; gap: 40px; list-style: none; }
        .nav-links a { color: var(--platinum); text-decoration: none; font-size: 14px; font-weight: 300; letter-spacing: 1px; transition: color 0.3s; position: relative; }
        .nav-links a::after { content: ''; position: absolute; bottom: -5px; left: 0; width: 0; height: 1px; background: linear-gradient(90deg, var(--champagne), var(--rose-gold)); transition: width 0.3s; }
        .nav-links a:hover::after { width: 100%; }
        .nav-links a:hover { color: var(--champagne); }
        .nav-actions { display: flex; gap: 20px; align-items: center; }
        .btn { padding: 10px 24px; border: 1px solid var(--glass-border); background: transparent; color: var(--platinum); font-family: 'Inter', sans-serif; font-size: 13px; font-weight: 400; letter-spacing: 1px; cursor: pointer; transition: all 0.3s; position: relative; overflow: hidden; }
        .btn::before { content: ''; position: absolute; top: 0; left: -100%; width: 100%; height: 100%; background: linear-gradient(90deg, transparent, rgba(247,231,206,0.1), transparent); transition: left 0.5s; }
        .btn:hover::before { left: 100%; }
        .btn:hover { border-color: var(--champagne); color: var(--champagne); }
        .btn-primary { background: linear-gradient(135deg, rgba(247,231,206,0.1), rgba(183,110,121,0.1)); border-color: rgba(247,231,206,0.3); }
        .hero { min-height: 100vh; display: flex; align-items: center; justify-content: center; position: relative; padding: 120px 40px 80px; }
        .hero-content { max-width: 1400px; width: 100%; display: grid; grid-template-columns: 1fr 1fr; gap: 80px; align-items: center; position: relative; z-index: 2; }
        .hero-text h1 { font-size: 72px; line-height: 1.1; margin-bottom: 24px; font-weight: 300; }
        .hero-text p { font-size: 18px; font-weight: 200; line-height: 1.8; color: rgba(229,228,226,0.7); margin-bottom: 40px; max-width: 500px; }
        .hero-cta { display: flex; gap: 20px; margin-bottom: 40px; }
        .search-bar { display: flex; max-width: 500px; background: rgba(255,255,255,0.03); border: 1px solid var(--glass-border); border-radius: 0; overflow: hidden; }
        .search-bar input { flex: 1; padding: 16px 24px; background: transparent; border: none; color: var(--platinum); font-family: 'Inter', sans-serif; font-size: 14px; font-weight: 300; outline: none; }
        .search-bar input::placeholder { color: rgba(229,228,226,0.4); }
        .search-bar button { padding: 16px 24px; background: linear-gradient(135deg, rgba(247,231,206,0.1), rgba(183,110,121,0.1)); border: none; color: var(--champagne); cursor: pointer; transition: all 0.3s; }
        .search-bar button:hover { background: linear-gradient(135deg, rgba(247,231,206,0.2), rgba(183,110,121,0.2)); }
        .gem-3d-container { width: 100%; height: 500px; position: relative; }
        #gem-canvas { width: 100%; height: 100%; }
        .stats-section { padding: 80px 40px; position: relative; z-index: 2; }
        .stats-grid { max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: repeat(4, 1fr); gap: 40px; }
        .stat-card { text-align: center; padding: 40px; border: 1px solid var(--glass-border); transition: all 0.3s; }
        .stat-card:hover { border-color: rgba(247,231,206,0.3); transform: translateY(-5px); }
        .stat-number { font-family: 'Cormorant Garamond', serif; font-size: 48px; font-weight: 300; color: var(--champagne); margin-bottom: 10px; }
        .stat-label { font-size: 13px; letter-spacing: 2px; text-transform: uppercase; color: rgba(229,228,226,0.6); }
        .featured-section { padding: 100px 40px; position: relative; z-index: 2; }
        .section-header { text-align: center; margin-bottom: 60px; }
        .section-header h2 { font-size: 48px; margin-bottom: 16px; }
        .section-header p { font-size: 16px; font-weight: 200; color: rgba(229,228,226,0.6); max-width: 600px; margin: 0 auto; }
        .gems-grid { max-width: 1400px; margin: 0 auto; display: grid; grid-template-columns: repeat(4, 1fr); gap: 30px; }
        .gem-card { border: 1px solid var(--glass-border); overflow: hidden; transition: all 0.4s; position: relative; cursor: pointer; }
        .gem-card:hover { border-color: rgba(247,231,206,0.3); transform: translateY(-10px); box-shadow: 0 20px 40px rgba(0,0,0,0.4); }
        .gem-card .gem-image { width: 100%; height: 280px; object-fit: cover; transition: transform 0.5s; }
        .gem-card:hover .gem-image { transform: scale(1.05); }
        .gem-card .gem-info { padding: 24px; }
        .gem-card .gem-name { font-family: 'Cormorant Garamond', serif; font-size: 24px; margin-bottom: 8px; }
        .gem-card .gem-type { font-size: 12px; letter-spacing: 2px; text-transform: uppercase; color: var(--rose-gold); margin-bottom: 12px; }
        .gem-card .gem-price { font-size: 20px; color: var(--champagne); font-weight: 400; }
        .gem-card .gem-origin { font-size: 12px; color: rgba(229,228,226,0.5); margin-top: 8px; }
        .wishlist-btn { position: absolute; top: 16px; right: 16px; width: 40px; height: 40px; border-radius: 50%; background: rgba(0,0,0,0.5); border: 1px solid var(--glass-border); color: var(--platinum); display: flex; align-items: center; justify-content: center; cursor: pointer; transition: all 0.3s; z-index: 10; }
        .wishlist-btn:hover, .wishlist-btn.active { background: rgba(183,110,121,0.3); border-color: var(--rose-gold); color: var(--rose-gold); }
        .ai-badge { position: absolute; top: 16px; left: 16px; padding: 6px 12px; background: linear-gradient(135deg, rgba(147,51,234,0.3), rgba(147,51,234,0.1)); border: 1px solid rgba(147,51,234,0.3); color: #c084fc; font-size: 11px; letter-spacing: 1px; z-index: 10; }
        .categories-section { padding: 80px 40px; position: relative; z-index: 2; }
        .categories-grid { max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: repeat(5, 1fr); gap: 20px; }
        .category-card { padding: 40px 20px; text-align: center; border: 1px solid var(--glass-border); transition: all 0.3s; cursor: pointer; }
        .category-card:hover { border-color: rgba(247,231,206,0.3); background: rgba(247,231,206,0.02); }
        .category-card i { font-size: 32px; color: var(--champagne); margin-bottom: 16px; display: block; }
        .category-card h3 { font-size: 18px; margin-bottom: 8px; }
        .category-card p { font-size: 12px; color: rgba(229,228,226,0.5); }
        .features-section { padding: 100px 40px; position: relative; z-index: 2; }
        .features-grid { max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: repeat(3, 1fr); gap: 40px; }
        .feature-card { padding: 40px; border: 1px solid var(--glass-border); text-align: center; transition: all 0.3s; }
        .feature-card:hover { border-color: rgba(247,231,206,0.3); transform: translateY(-5px); }
        .feature-card .feature-icon { width: 60px; height: 60px; margin: 0 auto 24px; display: flex; align-items: center; justify-content: center; border: 1px solid var(--glass-border); border-radius: 50%; font-size: 24px; color: var(--champagne); }
        .feature-card h3 { font-size: 24px; margin-bottom: 16px; }
        .feature-card p { font-size: 14px; font-weight: 200; line-height: 1.8; color: rgba(229,228,226,0.6); }
        .testimonials-section { padding: 100px 40px; position: relative; z-index: 2; }
        .testimonials-grid { max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: repeat(3, 1fr); gap: 40px; }
        .testimonial-card { padding: 40px; border: 1px solid var(--glass-border); position: relative; }
        .testimonial-card::before { content: '"'; font-family: 'Cormorant Garamond', serif; font-size: 80px; color: var(--champagne); opacity: 0.3; position: absolute; top: 20px; left: 30px; }
        .testimonial-card p { font-size: 16px; font-weight: 200; line-height: 1.8; color: rgba(229,228,226,0.8); margin-bottom: 24px; position: relative; z-index: 1; }
        .testimonial-author { display: flex; align-items: center; gap: 16px; }
        .testimonial-author img { width: 50px; height: 50px; border-radius: 50%; object-fit: cover; border: 2px solid var(--glass-border); }
        .testimonial-author-info h4 { font-size: 16px; margin-bottom: 4px; }
        .testimonial-author-info span { font-size: 12px; color: rgba(229,228,226,0.5); }
        .verified-badge { display: inline-flex; align-items: center; gap: 4px; padding: 4px 8px; background: rgba(34,197,94,0.1); border: 1px solid rgba(34,197,94,0.3); color: #4ade80; font-size: 10px; letter-spacing: 1px; margin-top: 8px; }
        .footer { padding: 80px 40px 40px; border-top: 1px solid var(--glass-border); position: relative; z-index: 2; }
        .footer-content { max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: 2fr 1fr 1fr 1fr; gap: 60px; }
        .footer-brand h3 { font-size: 28px; margin-bottom: 16px; }
        .footer-brand p { font-size: 14px; font-weight: 200; line-height: 1.8; color: rgba(229,228,226,0.6); margin-bottom: 24px; }
        .footer-social { display: flex; gap: 16px; }
        .footer-social a { width: 40px; height: 40px; border: 1px solid var(--glass-border); display: flex; align-items: center; justify-content: center; color: var(--platinum); transition: all 0.3s; }
        .footer-social a:hover { border-color: var(--champagne); color: var(--champagne); }
        .footer-links h4 { font-size: 16px; margin-bottom: 24px; color: var(--champagne); }
        .footer-links ul { list-style: none; }
        .footer-links li { margin-bottom: 12px; }
        .footer-links a { color: rgba(229,228,226,0.6); text-decoration: none; font-size: 14px; font-weight: 200; transition: color 0.3s; }
        .footer-links a:hover { color: var(--champagne); }
        .footer-bottom { max-width: 1200px; margin: 60px auto 0; padding-top: 30px; border-top: 1px solid var(--glass-border); display: flex; justify-content: space-between; align-items: center; }
        .footer-bottom p { font-size: 12px; color: rgba(229,228,226,0.4); }
        .security-badges { display: flex; gap: 20px; align-items: center; }
        .security-badges span { font-size: 11px; color: rgba(229,228,226,0.4); display: flex; align-items: center; gap: 6px; }
        .modal-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.8); backdrop-filter: blur(10px); display: none; align-items: center; justify-content: center; z-index: 2000; opacity: 0; transition: opacity 0.3s; }
        .modal-overlay.active { display: flex; opacity: 1; }
        .modal { background: var(--darker); border: 1px solid var(--glass-border); max-width: 500px; width: 90%; max-height: 90vh; overflow-y: auto; padding: 40px; position: relative; }
        .modal-close { position: absolute; top: 20px; right: 20px; background: none; border: none; color: var(--platinum); font-size: 24px; cursor: pointer; }
        .modal h2 { font-size: 32px; margin-bottom: 8px; }
        .modal p { font-size: 14px; color: rgba(229,228,226,0.6); margin-bottom: 30px; }
        .form-group { margin-bottom: 20px; }
        .form-group label { display: block; font-size: 12px; letter-spacing: 1px; text-transform: uppercase; color: rgba(229,228,226,0.6); margin-bottom: 8px; }
        .form-group input, .form-group select, .form-group textarea { width: 100%; padding: 14px 16px; background: rgba(255,255,255,0.03); border: 1px solid var(--glass-border); color: var(--platinum); font-family: 'Inter', sans-serif; font-size: 14px; font-weight: 300; outline: none; transition: border-color 0.3s; }
        .form-group input:focus, .form-group select:focus, .form-group textarea:focus { border-color: var(--champagne); }
        .form-group input::placeholder { color: rgba(229,228,226,0.3); }
        .social-login { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; margin-bottom: 24px; }
        .social-btn { padding: 12px; border: 1px solid var(--glass-border); background: transparent; color: var(--platinum); font-family: 'Inter', sans-serif; font-size: 13px; cursor: pointer; transition: all 0.3s; display: flex; align-items: center; justify-content: center; gap: 8px; }
        .social-btn:hover { border-color: var(--champagne); }
        .divider { text-align: center; margin: 24px 0; position: relative; }
        .divider::before { content: ''; position: absolute; top: 50%; left: 0; right: 0; height: 1px; background: var(--glass-border); }
        .divider span { background: var(--darker); padding: 0 16px; position: relative; font-size: 12px; color: rgba(229,228,226,0.4); }
        .otp-inputs { display: flex; gap: 12px; justify-content: center; margin-bottom: 30px; }
        .otp-inputs input { width: 50px; height: 60px; text-align: center; font-size: 24px; background: rgba(255,255,255,0.03); border: 1px solid var(--glass-border); color: var(--platinum); outline: none; }
        .otp-inputs input:focus { border-color: var(--champagne); }
        .user-dropdown { position: relative; }
        .user-menu { position: absolute; top: 100%; right: 0; margin-top: 10px; background: var(--darker); border: 1px solid var(--glass-border); min-width: 220px; display: none; z-index: 1001; }
        .user-menu.active { display: block; }
        .user-menu a { display: block; padding: 14px 20px; color: var(--platinum); text-decoration: none; font-size: 14px; font-weight: 300; transition: all 0.3s; border-bottom: 1px solid var(--glass-border); }
        .user-menu a:hover { background: rgba(247,231,206,0.05); color: var(--champagne); }
        .user-menu a:last-child { border-bottom: none; color: var(--rose-gold); }
        .cart-sidebar { position: fixed; top: 0; right: -400px; width: 400px; height: 100%; background: var(--darker); border-left: 1px solid var(--glass-border); z-index: 1500; transition: right 0.4s; overflow-y: auto; }
        .cart-sidebar.active { right: 0; }
        .cart-header { padding: 30px; border-bottom: 1px solid var(--glass-border); display: flex; justify-content: space-between; align-items: center; }
        .cart-header h3 { font-size: 24px; }
        .cart-close { background: none; border: none; color: var(--platinum); font-size: 24px; cursor: pointer; }
        .cart-items { padding: 20px 30px; }
        .cart-item { display: flex; gap: 16px; padding: 16px 0; border-bottom: 1px solid var(--glass-border); }
        .cart-item img { width: 80px; height: 80px; object-fit: cover; border: 1px solid var(--glass-border); }
        .cart-item-info h4 { font-size: 16px; margin-bottom: 4px; }
        .cart-item-info p { font-size: 14px; color: var(--champagne); margin-bottom: 8px; }
        .cart-item-actions { display: flex; gap: 12px; }
        .cart-item-actions button { background: none; border: 1px solid var(--glass-border); color: var(--platinum); padding: 4px 12px; font-size: 12px; cursor: pointer; transition: all 0.3s; }
        .cart-item-actions button:hover { border-color: var(--rose-gold); color: var(--rose-gold); }
        .cart-footer { padding: 30px; border-top: 1px solid var(--glass-border); position: sticky; bottom: 0; background: var(--darker); }
        .cart-total { display: flex; justify-content: space-between; margin-bottom: 20px; font-size: 18px; }
        .cart-total span:last-child { color: var(--champagne); font-weight: 500; }
        .chat-widget { position: fixed; bottom: 30px; left: 30px; z-index: 1000; }
        .chat-toggle { width: 60px; height: 60px; border-radius: 50%; background: linear-gradient(135deg, rgba(247,231,206,0.2), rgba(183,110,121,0.2)); border: 1px solid var(--glass-border); color: var(--champagne); font-size: 24px; cursor: pointer; display: flex; align-items: center; justify-content: center; transition: all 0.3s; }
        .chat-toggle:hover { transform: scale(1.1); box-shadow: 0 0 30px rgba(247,231,206,0.2); }
        .chat-window { position: absolute; bottom: 80px; left: 0; width: 350px; height: 450px; background: var(--darker); border: 1px solid var(--glass-border); display: none; flex-direction: column; }
        .chat-window.active { display: flex; }
        .chat-header { padding: 20px; border-bottom: 1px solid var(--glass-border); display: flex; justify-content: space-between; align-items: center; }
        .chat-header h4 { font-size: 16px; }
        .chat-messages { flex: 1; overflow-y: auto; padding: 20px; }
        .chat-message { margin-bottom: 16px; max-width: 80%; }
        .chat-message.sent { margin-left: auto; text-align: right; }
        .chat-message p { padding: 12px 16px; background: rgba(255,255,255,0.03); border: 1px solid var(--glass-border); font-size: 13px; line-height: 1.6; }
        .chat-message.sent p { background: rgba(247,231,206,0.05); border-color: rgba(247,231,206,0.2); }
        .chat-input { display: flex; padding: 16px; border-top: 1px solid var(--glass-border); }
        .chat-input input { flex: 1; padding: 12px; background: rgba(255,255,255,0.03); border: 1px solid var(--glass-border); color: var(--platinum); outline: none; font-family: 'Inter', sans-serif; }
        .chat-input button { padding: 12px 20px; background: linear-gradient(135deg, rgba(247,231,206,0.1), rgba(183,110,121,0.1)); border: 1px solid var(--glass-border); color: var(--champagne); cursor: pointer; margin-left: 8px; }
        .viewer-modal .modal { max-width: 900px; width: 95%; }
        .viewer-content { display: grid; grid-template-columns: 1fr 1fr; gap: 40px; }
        .viewer-3d { height: 400px; border: 1px solid var(--glass-border); position: relative; }
        .viewer-controls { display: flex; gap: 10px; margin-top: 16px; }
        .viewer-controls button { padding: 8px 16px; background: rgba(255,255,255,0.03); border: 1px solid var(--glass-border); color: var(--platinum); font-size: 12px; cursor: pointer; transition: all 0.3s; }
        .viewer-controls button:hover { border-color: var(--champagne); color: var(--champagne); }
        .viewer-gallery { display: flex; gap: 10px; margin-top: 16px; }
        .viewer-gallery img { width: 80px; height: 80px; object-fit: cover; border: 1px solid var(--glass-border); cursor: pointer; transition: all 0.3s; }
        .viewer-gallery img:hover, .viewer-gallery img.active { border-color: var(--champagne); }
        .viewer-info h3 { font-size: 28px; margin-bottom: 8px; }
        .viewer-info .price { font-size: 32px; color: var(--champagne); margin-bottom: 24px; }
        .viewer-info .details { margin-bottom: 24px; }
        .viewer-info .details p { display: flex; justify-content: space-between; padding: 12px 0; border-bottom: 1px solid var(--glass-border); font-size: 14px; }
        .viewer-info .details p span:first-child { color: rgba(229,228,226,0.6); }
        .anti-theft-warning { padding: 16px; background: rgba(239,68,68,0.05); border: 1px solid rgba(239,68,68,0.3); color: #ef4444; font-size: 12px; margin-bottom: 20px; display: flex; align-items: center; gap: 10px; }
        .blockchain-badge { display: inline-flex; align-items: center; gap: 8px; padding: 8px 16px; background: rgba(34,197,94,0.05); border: 1px solid rgba(34,197,94,0.3); color: #4ade80; font-size: 12px; margin-bottom: 20px; }
        .admin-panel { display: none; padding: 100px 40px; position: relative; z-index: 2; }
        .admin-panel.active { display: block; }
        .admin-tabs { display: flex; gap: 0; border-bottom: 1px solid var(--glass-border); margin-bottom: 40px; }
        .admin-tab { padding: 16px 32px; background: none; border: none; border-bottom: 2px solid transparent; color: rgba(229,228,226,0.6); font-family: 'Inter', sans-serif; font-size: 14px; cursor: pointer; transition: all 0.3s; }
        .admin-tab.active { color: var(--champagne); border-bottom-color: var(--champagne); }
        .admin-tab:hover { color: var(--platinum); }
        .admin-table { width: 100%; border-collapse: collapse; }
        .admin-table th, .admin-table td { padding: 16px; text-align: left; border-bottom: 1px solid var(--glass-border); font-size: 14px; }
        .admin-table th { color: var(--champagne); font-weight: 400; letter-spacing: 1px; text-transform: uppercase; font-size: 12px; }
        .admin-table td { color: rgba(229,228,226,0.8); }
        .status-badge { padding: 4px 12px; font-size: 11px; letter-spacing: 1px; }
        .status-pending { background: rgba(234,179,8,0.1); border: 1px solid rgba(234,179,8,0.3); color: #fbbf24; }
        .status-approved { background: rgba(34,197,94,0.1); border: 1px solid rgba(34,197,94,0.3); color: #4ade80; }
        .status-rejected { background: rgba(239,68,68,0.1); border: 1px solid rgba(239,68,68,0.3); color: #ef4444; }
        .action-btns { display: flex; gap: 8px; }
        .action-btns button { padding: 6px 12px; font-size: 11px; background: none; border: 1px solid var(--glass-border); color: var(--platinum); cursor: pointer; transition: all 0.3s; }
        .action-btns button:hover { border-color: var(--champagne); color: var(--champagne); }
        .action-btns .reject:hover { border-color: var(--rose-gold); color: var(--rose-gold); }
        .seller-dashboard { display: none; padding: 100px 40px; position: relative; z-index: 2; }
        .seller-dashboard.active { display: block; }
        .dashboard-stats { display: grid; grid-template-columns: repeat(4, 1fr); gap: 24px; margin-bottom: 40px; }
        .dashboard-stat-card { padding: 30px; border: 1px solid var(--glass-border); }
        .dashboard-stat-card h4 { font-size: 14px; color: rgba(229,228,226,0.6); margin-bottom: 12px; font-family: 'Inter', sans-serif; font-weight: 300; }
        .dashboard-stat-card .value { font-family: 'Cormorant Garamond', serif; font-size: 36px; color: var(--champagne); }
        .dashboard-stat-card .change { font-size: 12px; color: #4ade80; margin-top: 8px; }
        .dashboard-chart { border: 1px solid var(--glass-border); padding: 30px; margin-bottom: 40px; }
        .dashboard-chart h3 { font-size: 24px; margin-bottom: 24px; }
        .dashboard-table-container { border: 1px solid var(--glass-border); padding: 30px; }
        .dashboard-table-container h3 { font-size: 24px; margin-bottom: 24px; }
        .upload-zone { border: 2px dashed var(--glass-border); padding: 40px; text-align: center; transition: all 0.3s; cursor: pointer; }
        .upload-zone:hover { border-color: var(--champagne); background: rgba(247,231,206,0.02); }
        .upload-zone i { font-size: 40px; color: var(--champagne); margin-bottom: 16px; }
        .upload-zone p { font-size: 14px; color: rgba(229,228,226,0.6); }
        .calc-result { padding: 24px; background: rgba(247,231,206,0.03); border: 1px solid var(--glass-border); margin-top: 24px; text-align: center; }
        .calc-result .price { font-family: 'Cormorant Garamond', serif; font-size: 48px; color: var(--champagne); }
        .lang-selector { position: relative; }
        .lang-menu { position: absolute; top: 100%; right: 0; margin-top: 10px; background: var(--darker); border: 1px solid var(--glass-border); display: none; z-index: 1001; }
        .lang-menu.active { display: block; }
        .lang-menu button { display: block; width: 100%; padding: 12px 20px; background: none; border: none; border-bottom: 1px solid var(--glass-border); color: var(--platinum); font-family: 'Inter', sans-serif; font-size: 13px; cursor: pointer; transition: all 0.3s; text-align: left; }
        .lang-menu button:hover { background: rgba(247,231,206,0.05); color: var(--champagne); }
        .lang-menu button:last-child { border-bottom: none; }
        .pwa-btn { position: fixed; bottom: 30px; right: 30px; padding: 14px 24px; background: linear-gradient(135deg, rgba(247,231,206,0.15), rgba(183,110,121,0.15)); border: 1px solid var(--glass-border); color: var(--champagne); font-family: 'Inter', sans-serif; font-size: 13px; cursor: pointer; z-index: 999; display: none; animation: slideUp 0.5s ease; }
        .pwa-btn.visible { display: block; }
        @keyframes slideUp { from { transform: translateY(100px); opacity: 0; } to { transform: translateY(0); opacity: 1; } }
        .mobile-menu-btn { display: none; background: none; border: none; color: var(--platinum); font-size: 24px; cursor: pointer; }
        .mobile-menu { display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: var(--darker); z-index: 999; padding: 80px 40px; }
        .mobile-menu.active { display: block; }
        .mobile-menu a { display: block; padding: 20px 0; color: var(--platinum); text-decoration: none; font-size: 24px; font-family: 'Cormorant Garamond', serif; border-bottom: 1px solid var(--glass-border); }
        .screenshot-warning { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.95); display: none; align-items: center; justify-content: center; z-index: 3000; flex-direction: column; text-align: center; padding: 40px; }
        .screenshot-warning.active { display: flex; }
        .screenshot-warning i { font-size: 64px; color: #ef4444; margin-bottom: 24px; }
        .screenshot-warning h2 { font-size: 36px; margin-bottom: 16px; color: #ef4444; }
        .screenshot-warning p { font-size: 16px; color: rgba(229,228,226,0.7); max-width: 500px; }
        @media (max-width: 1024px) { .hero-content { grid-template-columns: 1fr; text-align: center; } .hero-text p { margin: 0 auto 40px; } .hero-cta { justify-content: center; } .search-bar { margin: 0 auto; } .gems-grid { grid-template-columns: repeat(2, 1fr); } .categories-grid { grid-template-columns: repeat(3, 1fr); } .stats-grid { grid-template-columns: repeat(2, 1fr); } .features-grid { grid-template-columns: repeat(2, 1fr); } .testimonials-grid { grid-template-columns: 1fr; } .footer-content { grid-template-columns: 1fr 1fr; } .dashboard-stats { grid-template-columns: repeat(2, 1fr); } }
        @media (max-width: 768px) { .nav-links { display: none; } .mobile-menu-btn { display: block; } .hero-text h1 { font-size: 42px; } .gems-grid { grid-template-columns: 1fr; } .categories-grid { grid-template-columns: repeat(2, 1fr); } .stats-grid { grid-template-columns: 1fr; } .features-grid { grid-template-columns: 1fr; } .footer-content { grid-template-columns: 1fr; } .viewer-content { grid-template-columns: 1fr; } .dashboard-stats { grid-template-columns: 1fr; } .admin-tabs { overflow-x: auto; } .cart-sidebar { width: 100%; right: -100%; } }
        .fade-in { opacity: 0; transform: translateY(30px); transition: all 0.8s ease; }
        .fade-in.visible { opacity: 1; transform: translateY(0); }
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: var(--darker); }
        ::-webkit-scrollbar-thumb { background: var(--glass-border); }
        ::-webkit-scrollbar-thumb:hover { background: rgba(247,231,206,0.2); }
    </style>
<base target="_blank">
</head>
<body>
    <div class="bg-grid"></div>
    <canvas id="particle-canvas"></canvas>

    <nav class="navbar" id="navbar">
        <div class="nav-container">
            <a href="#" class="logo" onclick="showHome()">FITGEM</a>
            <ul class="nav-links">
                <li><a href="#" onclick="showHome()">Home</a></li>
                <li><a href="#" onclick="showHome()">Gemstones</a></li>
                <li><a href="#" onclick="showHome()">Categories</a></li>
                <li><a href="#" onclick="showHome()">Features</a></li>
                <li><a href="#" onclick="showSellerDashboard()">Seller Dashboard</a></li>
                <li><a href="#" onclick="showAdminPanel()">Admin</a></li>
            </ul>
            <div class="nav-actions">
                <div class="lang-selector">
                    <button class="btn" onclick="toggleLangMenu()">EN <i class="fas fa-chevron-down" style="margin-left:6px;font-size:10px;"></i></button>
                    <div class="lang-menu" id="langMenu">
                        <button onclick="changeLang('EN')">English</button>
                        <button onclick="changeLang('ES')">Español</button>
                        <button onclick="changeLang('FR')">Français</button>
                        <button onclick="changeLang('DE')">Deutsch</button>
                        <button onclick="changeLang('ZH')">中文</button>
                        <button onclick="changeLang('AR')">العربية</button>
                    </div>
                </div>
                <button class="btn" onclick="openCart()">
                    <i class="fas fa-shopping-cart"></i> <span id="cartCount">0</span>
                </button>
                <button class="btn" onclick="openCalculator()">
                    <i class="fas fa-calculator"></i>
                </button>
                <button class="btn" onclick="toggleToneMenu()" id="toneBtn">
                    <i class="fas fa-palette"></i>
                </button>
                <div class="lang-menu" id="toneMenu" style="right:0;left:auto;">
                    <button onclick="switchTone('luxury');toggleToneMenu()">Luxury (Default)</button>
                    <button onclick="switchTone('cram');toggleToneMenu()">AI Cram</button>
                </div>
                <button class="btn" onclick="toggleMusic()" id="musicBtn">
                    <i class="fas fa-music" id="musicIcon"></i>
                </button>

                <div class="user-dropdown" id="userDropdown" style="display:none;">
                    <button class="btn" onclick="toggleUserMenu()">
                        <i class="fas fa-user"></i> <span id="userName">User</span>
                    </button>
                    <div class="user-menu" id="userMenu">
                        <a href="#" onclick="showProfile()">Profile</a>
                        <a href="#" onclick="showOrders()">Orders</a>
                        <a href="#" onclick="showWishlist()">Wishlist</a>
                        <a href="#" onclick="showSellerDashboard()">Seller Dashboard</a>
                        <a href="#" onclick="showKYC()">KYC Verification</a>
                        <a href="#" onclick="logout()">Logout</a>
                    </div>
                </div>
                <button class="btn btn-primary" id="loginBtn" onclick="openLogin()">Sign In</button>
                <button class="mobile-menu-btn" onclick="toggleMobileMenu()">
                    <i class="fas fa-bars"></i>
                </button>
            </div>
        </div>
    </nav>

    <div class="mobile-menu" id="mobileMenu">
        <button style="position:absolute;top:30px;right:40px;background:none;border:none;color:var(--platinum);font-size:24px;" onclick="toggleMobileMenu()">
            <i class="fas fa-times"></i>
        </button>
        <a href="#" onclick="toggleMobileMenu();showHome()">Home</a>
        <a href="#" onclick="toggleMobileMenu();showHome()">Gemstones</a>
        <a href="#" onclick="toggleMobileMenu();showHome()">Categories</a>
        <a href="#" onclick="toggleMobileMenu();showHome()">Features</a>
        <a href="#" onclick="toggleMobileMenu();showSellerDashboard()">Seller Dashboard</a>
        <a href="#" onclick="toggleMobileMenu();showAdminPanel()">Admin Panel</a>
        <a href="#" onclick="toggleMobileMenu();openLogin()">Sign In</a>
    </div>

    <main id="mainContent">
        <section class="hero" id="home">
            <div class="hero-content">
                <div class="hero-text">
                    <h1 class="shimmer">The World's Finest<br>Sri Lankan Gemstones</h1>
                    <p>Discover certified precious gems from the heart of Ceylon. Each stone tells a story of rarity, beauty, and timeless elegance.</p>
                    <div class="hero-cta">
                        <button class="btn btn-primary" onclick="scrollToGems()">Buy Gems</button>
                        <button class="btn" onclick="showSellerDashboard()">Sell Gems</button>
                    </div>
                    <div class="search-bar">
                        <input type="text" placeholder="Search by gem type, color, origin..." id="searchInput">
                        <button onclick="searchGems()"><i class="fas fa-search"></i></button>
                    </div>
                </div>
                <div class="gem-3d-container">
                    <canvas id="gem-canvas"></canvas>
                </div>
            </div>
        </section>

        <section class="stats-section">
            <div class="stats-grid">
                <div class="stat-card fade-in">
                    <div class="stat-number" data-target="12500">0</div>
                    <div class="stat-label">Certified Gems</div>
                </div>
                <div class="stat-card fade-in">
                    <div class="stat-number" data-target="850">0</div>
                    <div class="stat-label">Verified Sellers</div>
                </div>
                <div class="stat-card fade-in">
                    <div class="stat-number" data-target="99">0</div>
                    <div class="stat-label">% Satisfaction</div>
                </div>
                <div class="stat-card fade-in">
                    <div class="stat-number" data-target="45">0</div>
                    <div class="stat-label">Countries Served</div>
                </div>
            </div>
        </section>

        <section class="featured-section" id="gems">
            <div class="section-header">
                <h2 class="shimmer">Featured Gemstones</h2>
                <p>Handpicked selection of the finest Sri Lankan gems, each certified and ethically sourced</p>
            </div>
            <div class="gems-grid" id="gemsGrid"></div>
        </section>

        <section class="categories-section" id="categories">
            <div class="section-header">
                <h2 class="shimmer">Browse Categories</h2>
                <p>Explore our extensive collection by gemstone type</p>
            </div>
            <div class="categories-grid">
                <div class="category-card" onclick="filterCategory('Sapphire')">
                    <i class="fas fa-gem"></i>
                    <h3>Sapphire</h3>
                    <p>2,400 listings</p>
                </div>
                <div class="category-card" onclick="filterCategory('Ruby')">
                    <i class="fas fa-gem"></i>
                    <h3>Ruby</h3>
                    <p>1,200 listings</p>
                </div>
                <div class="category-card" onclick="filterCategory('Emerald')">
                    <i class="fas fa-gem"></i>
                    <h3>Emerald</h3>
                    <p>890 listings</p>
                </div>
                <div class="category-card" onclick="filterCategory('Topaz')">
                    <i class="fas fa-gem"></i>
                    <h3>Topaz</h3>
                    <p>650 listings</p>
                </div>
                <div class="category-card" onclick="filterCategory('Cat's Eye')">
                    <i class="fas fa-eye"></i>
                    <h3>Cat's Eye</h3>
                    <p>340 listings</p>
                </div>
                <div class="category-card" onclick="filterCategory('Garnet')">
                    <i class="fas fa-gem"></i>
                    <h3>Garnet</h3>
                    <p>520 listings</p>
                </div>
                <div class="category-card" onclick="filterCategory('Aquamarine')">
                    <i class="fas fa-water"></i>
                    <h3>Aquamarine</h3>
                    <p>280 listings</p>
                </div>
                <div class="category-card" onclick="filterCategory('Alexandrite')">
                    <i class="fas fa-gem"></i>
                    <h3>Alexandrite</h3>
                    <p>150 listings</p>
                </div>
                <div class="category-card" onclick="filterCategory('Moonstone')">
                    <i class="fas fa-moon"></i>
                    <h3>Moonstone</h3>
                    <p>420 listings</p>
                </div>
                <div class="category-card" onclick="filterCategory('Rare Gems')">
                    <i class="fas fa-star"></i>
                    <h3>Rare Gems</h3>
                    <p>180 listings</p>
                </div>
            </div>
        </section>

        <section class="features-section" id="features">
            <div class="section-header">
                <h2 class="shimmer">Why Choose FITGEM</h2>
                <p>The most trusted gemstone marketplace in Sri Lanka</p>
            </div>
            <div class="features-grid">
                <div class="feature-card fade-in">
                    <div class="feature-icon"><i class="fas fa-certificate"></i></div>
                    <h3>Certified Authenticity</h3>
                    <p>Every gemstone comes with GIA, GII, or CGL certification. Blockchain verification ensures traceability from mine to market.</p>
                </div>
                <div class="feature-card fade-in">
                    <div class="feature-icon"><i class="fas fa-shield-alt"></i></div>
                    <h3>Secure Transactions</h3>
                    <p>AES-256 encryption, PCI DSS compliance, and escrow protection. Your payments and data are always secure.</p>
                </div>
                <div class="feature-card fade-in">
                    <div class="feature-icon"><i class="fas fa-video"></i></div>
                    <h3>Video Inspection</h3>
                    <p>Schedule live video calls with sellers via Zoom or Google Meet. Inspect gems in real-time before purchase.</p>
                </div>
                <div class="feature-card fade-in">
                    <div class="feature-icon"><i class="fas fa-undo"></i></div>
                    <h3>Buyer Protection</h3>
                    <p>30-day return policy with full refund guarantee. Not satisfied? Return it hassle-free.</p>
                </div>
                <div class="feature-card fade-in">
                    <div class="feature-icon"><i class="fas fa-globe"></i></div>
                    <h3>Global Shipping</h3>
                    <p>Insured international shipping to 45+ countries. Real-time tracking with FedEx and DHL partnerships.</p>
                </div>
                <div class="feature-card fade-in">
                    <div class="feature-icon"><i class="fas fa-robot"></i></div>
                    <h3>AI Recommendations</h3>
                    <p>Our AI analyzes your preferences to suggest perfect gems. Personalized curation for every collector.</p>
                </div>
            </div>
        </section>

        <section class="testimonials-section">
            <div class="section-header">
                <h2 class="shimmer">Client Testimonials</h2>
                <p>What our collectors say about FITGEM</p>
            </div>
            <div class="testimonials-grid">
                <div class="testimonial-card fade-in">
                    <p>The 5-carat Ceylon sapphire I purchased exceeded all expectations. The certification, video inspection, and seamless shipping made this the best gem buying experience I've ever had.</p>
                    <div class="testimonial-author">
                        <img src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=100&h=100&fit=crop&crop=face" alt="James">
                        <div class="testimonial-author-info">
                            <h4>James Richardson</h4>
                            <span>Collector, New York</span>
                            <div class="verified-badge"><i class="fas fa-check-circle"></i> Verified Purchase</div>
                        </div>
                    </div>
                </div>
                <div class="testimonial-card fade-in">
                    <p>As a seller, FITGEM has transformed my business. The dashboard analytics, secure payments, and global reach have increased my sales by 300% in just six months.</p>
                    <div class="testimonial-author">
                        <img src="https://images.unsplash.com/photo-1494790108377-be9c29b29330?w=100&h=100&fit=crop&crop=face" alt="Priya">
                        <div class="testimonial-author-info">
                            <h4>Priya Fernando</h4>
                            <span>Seller, Ratnapura</span>
                            <div class="verified-badge"><i class="fas fa-check-circle"></i> Verified Seller</div>
                        </div>
                    </div>
                </div>
                <div class="testimonial-card fade-in">
                    <p>The blockchain verification and anti-theft protection give me complete confidence. I can trace my alexandrite from the Sri Lankan mine to my collection in Tokyo.</p>
                    <div class="testimonial-author">
                        <img src="https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=100&h=100&fit=crop&crop=face" alt="Kenji">
                        <div class="testimonial-author-info">
                            <h4>Kenji Tanaka</h4>
                            <span>Investor, Tokyo</span>
                            <div class="verified-badge"><i class="fas fa-check-circle"></i> Verified Purchase</div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <div class="seller-dashboard" id="sellerDashboard">
        <div class="section-header">
            <h2 class="shimmer">Seller Dashboard</h2>
            <p>Manage your listings, track sales, and grow your business</p>
        </div>
        <div style="max-width:1200px;margin:0 auto;">
            <div class="dashboard-stats">
                <div class="dashboard-stat-card">
                    <h4>Active Listings</h4>
                    <div class="value">24</div>
                    <div class="change"><i class="fas fa-arrow-up"></i> +12% this month</div>
                </div>
                <div class="dashboard-stat-card">
                    <h4>Total Sales</h4>
                    <div class="value">$48,250</div>
                    <div class="change"><i class="fas fa-arrow-up"></i> +28% this month</div>
                </div>
                <div class="dashboard-stat-card">
                    <h4>Total Views</h4>
                    <div class="value">12,450</div>
                    <div class="change"><i class="fas fa-arrow-up"></i> +45% this month</div>
                </div>
                <div class="dashboard-stat-card">
                    <h4>Rating</h4>
                    <div class="value">4.9</div>
                    <div class="change"><i class="fas fa-star"></i> 128 reviews</div>
                </div>
            </div>
            <div class="dashboard-chart">
                <h3>Sales Analytics</h3>
                <canvas id="salesChart" height="100"></canvas>
            </div>
            <div class="dashboard-table-container">
                <h3>My Listings</h3>
                <div style="overflow-x:auto;">
                    <table class="admin-table">
                        <thead>
                            <tr><th>Gem</th><th>Type</th><th>Price</th><th>Views</th><th>Status</th><th>Actions</th></tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>Blue Sapphire 5.2ct</td><td>Sapphire</td><td>$12,500</td><td>1,240</td>
                                <td><span class="status-badge status-approved">Active</span></td>
                                <td><div class="action-btns"><button>Edit</button><button>Promote</button><button class="reject">Delete</button></div></td>
                            </tr>
                            <tr>
                                <td>Ruby 3.1ct Pigeon Blood</td><td>Ruby</td><td>$8,900</td><td>890</td>
                                <td><span class="status-badge status-approved">Active</span></td>
                                <td><div class="action-btns"><button>Edit</button><button>Promote</button><button class="reject">Delete</button></div></td>
                            </tr>
                            <tr>
                                <td>Cat's Eye 4.5ct</td><td>Cat's Eye</td><td>$3,200</td><td>456</td>
                                <td><span class="status-badge status-pending">Pending</span></td>
                                <td><div class="action-btns"><button>Edit</button><button>Promote</button><button class="reject">Delete</button></div></td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
            <div style="margin-top:40px;display:flex;gap:16px;">
                <button class="btn btn-primary" onclick="openAddListing()"><i class="fas fa-plus"></i> Add New Listing</button>
                <button class="btn" onclick="exportAnalytics()"><i class="fas fa-download"></i> Export Analytics</button>
                <button class="btn" onclick="promoteListing()"><i class="fas fa-bullhorn"></i> Promote Listing</button>
            </div>
        </div>
    </div>

    <div class="admin-panel" id="adminPanel">
        <div class="section-header">
            <h2 class="shimmer">Admin Panel</h2>
            <p>Manage users, listings, certificates, and payments</p>
        </div>
        <div style="max-width:1200px;margin:0 auto;">
            <div class="admin-tabs">
                <button class="admin-tab active" onclick="switchTab('users')">Users</button>
                <button class="admin-tab" onclick="switchTab('listings')">Listings</button>
                <button class="admin-tab" onclick="switchTab('certificates')">Certificates</button>
                <button class="admin-tab" onclick="switchTab('payments')">Payments</button>
            </div>
            <div id="tab-users" class="tab-content">
                <div style="overflow-x:auto;">
                    <table class="admin-table">
                        <thead><tr><th>User</th><th>Email</th><th>Type</th><th>Status</th><th>KYC</th><th>Actions</th></tr></thead>
                        <tbody>
                            <tr><td>Menuka Kodithuwakku</td><td>menuka@fitgem.lk</td><td>Admin</td><td><span class="status-badge status-approved">Active</span></td><td><span class="status-badge status-approved">Verified</span></td><td><div class="action-btns"><button>View</button><button class="reject">Suspend</button></div></td></tr>
                            <tr><td>Priya Fernando</td><td>priya@ratnapura.lk</td><td>Seller</td><td><span class="status-badge status-approved">Active</span></td><td><span class="status-badge status-pending">Pending</span></td><td><div class="action-btns"><button>Approve</button><button class="reject">Reject</button></div></td></tr>
                            <tr><td>James Richardson</td><td>james@collector.com</td><td>Buyer</td><td><span class="status-badge status-approved">Active</span></td><td><span class="status-badge status-approved">Verified</span></td><td><div class="action-btns"><button>View</button><button class="reject">Block</button></div></td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
            <div id="tab-listings" class="tab-content" style="display:none;">
                <div style="overflow-x:auto;">
                    <table class="admin-table">
                        <thead><tr><th>Gem</th><th>Seller</th><th>Price</th><th>Submitted</th><th>Status</th><th>Actions</th></tr></thead>
                        <tbody>
                            <tr><td>Blue Sapphire 5.2ct</td><td>Priya Fernando</td><td>$12,500</td><td>2026-05-28</td><td><span class="status-badge status-pending">Pending</span></td><td><div class="action-btns"><button>Approve</button><button class="reject">Reject</button></div></td></tr>
                            <tr><td>Ruby 3.1ct Pigeon Blood</td><td>Sunil Perera</td><td>$8,900</td><td>2026-05-27</td><td><span class="status-badge status-approved">Approved</span></td><td><div class="action-btns"><button>View</button><button class="reject">Remove</button></div></td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
            <div id="tab-certificates" class="tab-content" style="display:none;">
                <div style="margin-bottom:24px;">
                    <div class="search-bar" style="max-width:400px;">
                        <input type="text" placeholder="Search certificate number...">
                        <button><i class="fas fa-search"></i></button>
                    </div>
                </div>
                <div style="overflow-x:auto;">
                    <table class="admin-table">
                        <thead><tr><th>Cert #</th><th>Gem</th><th>Lab</th><th>Blockchain</th><th>Status</th></tr></thead>
                        <tbody>
                            <tr><td>GIA-2026-88421</td><td>Blue Sapphire 5.2ct</td><td>GIA</td><td><span class="blockchain-badge"><i class="fas fa-link"></i> Verified</span></td><td><span class="status-badge status-approved">Valid</span></td></tr>
                            <tr><td>CGL-2026-55231</td><td>Ruby 3.1ct</td><td>CGL</td><td><span class="blockchain-badge"><i class="fas fa-link"></i> Verified</span></td><td><span class="status-badge status-approved">Valid</span></td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
            <div id="tab-payments" class="tab-content" style="display:none;">
                <div class="dashboard-chart" style="margin-bottom:40px;">
                    <h3>Payment Volume</h3>
                    <canvas id="paymentChart" height="100"></canvas>
                </div>
                <div style="overflow-x:auto;">
                    <table class="admin-table">
                        <thead><tr><th>Transaction ID</th><th>Buyer</th><th>Seller</th><th>Amount</th><th>Method</th><th>Status</th></tr></thead>
                        <tbody>
                            <tr><td>TXN-2026-001245</td><td>James Richardson</td><td>Priya Fernando</td><td>$12,500</td><td>Stripe</td><td><span class="status-badge status-approved">Completed</span></td></tr>
                            <tr><td>TXN-2026-001244</td><td>Kenji Tanaka</td><td>Sunil Perera</td><td>$8,900</td><td>PayPal</td><td><span class="status-badge status-pending">Pending</span></td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <footer class="footer">
        <div class="footer-content">
            <div class="footer-brand">
                <h3>FITGEM</h3>
                <p>Sri Lanka's premier gemstone marketplace. Connecting collectors with certified precious gems since 2026. Every stone tells a story of rarity and beauty.</p>
                <div class="footer-social">
                    <a href="#"><i class="fab fa-facebook-f"></i></a>
                    <a href="#"><i class="fab fa-instagram"></i></a>
                    <a href="#"><i class="fab fa-twitter"></i></a>
                    <a href="#"><i class="fab fa-linkedin-in"></i></a>
                </div>
            </div>
            <div class="footer-links">
                <h4>Marketplace</h4>
                <ul>
                    <li><a href="#">All Gemstones</a></li>
                    <li><a href="#">Sellers</a></li>
                    <li><a href="#">Auctions</a></li>
                    <li><a href="#">Price Calculator</a></li>
                </ul>
            </div>
            <div class="footer-links">
                <h4>Company</h4>
                <ul>
                    <li><a href="#">About Us</a></li>
                    <li><a href="#">Careers</a></li>
                    <li><a href="#">Press</a></li>
                    <li><a href="#">Contact</a></li>
                </ul>
            </div>
            <div class="footer-links">
                <h4>Support</h4>
                <ul>
                    <li><a href="#">Help Center</a></li>
                    <li><a href="#">Safety</a></li>
                    <li><a href="#">Terms of Service</a></li>
                    <li><a href="#">Privacy Policy</a></li>
                </ul>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2026 FITGEM. All rights reserved. Founded by Menuka Kodithuwakku.</p>
            <div class="security-badges">
                <span><i class="fas fa-lock"></i> AES-256</span>
                <span><i class="fas fa-shield-alt"></i> PCI DSS</span>
                <span><i class="fas fa-certificate"></i> SSL Secure</span>
                <span><i class="fas fa-link"></i> Blockchain</span>
            </div>
        </div>
    </footer>

    <!-- Login Modal -->
    <div class="modal-overlay" id="loginModal">
        <div class="modal">
            <button class="modal-close" onclick="closeModal('loginModal')">&times;</button>
            <h2 class="shimmer">Welcome Back</h2>
            <p>Sign in to your FITGEM account</p>
            <div class="social-login">
                <button class="social-btn" onclick="socialLogin('google')">
                    <i class="fab fa-google"></i> Google
                </button>
                <button class="social-btn" onclick="socialLogin('facebook')">
                    <i class="fab fa-facebook-f"></i> Facebook
                </button>
            </div>
            <div class="divider"><span>or sign in with email</span></div>
            <form onsubmit="handleLogin(event)">
                <div class="form-group">
                    <label>Email Address</label>
                    <input type="email" placeholder="your@email.com" required>
                </div>
                <div class="form-group">
                    <label>Password</label>
                    <input type="password" placeholder="Enter password" required>
                </div>
                <button type="submit" class="btn btn-primary" style="width:100%;margin-top:10px;">Sign In</button>
            </form>
            <p style="text-align:center;margin-top:24px;font-size:14px;">
                Don't have an account? <a href="#" onclick="switchToRegister()" style="color:var(--champagne);">Register</a>
            </p>
        </div>
    </div>

    <!-- Register Modal -->
    <div class="modal-overlay" id="registerModal">
        <div class="modal">
            <button class="modal-close" onclick="closeModal('registerModal')">&times;</button>
            <h2 class="shimmer">Create Account</h2>
            <p>Join FITGEM as a buyer or seller</p>
            <div class="social-login">
                <button class="social-btn" onclick="socialLogin('google')">
                    <i class="fab fa-google"></i> Google
                </button>
                <button class="social-btn" onclick="socialLogin('facebook')">
                    <i class="fab fa-facebook-f"></i> Facebook
                </button>
            </div>
            <div class="divider"><span>or register with email</span></div>
            <form onsubmit="handleRegister(event)">
                <div class="form-group">
                    <label>Full Name</label>
                    <input type="text" placeholder="Your full name" required>
                </div>
                <div class="form-group">
                    <label>Email Address</label>
                    <input type="email" placeholder="your@email.com" required>
                </div>
                <div class="form-group">
                    <label>Phone Number</label>
                    <input type="tel" placeholder="+94 77 123 4567" required>
                </div>
                <div class="form-group">
                    <label>Account Type</label>
                    <select required>
                        <option value="">Select type</option>
                        <option value="buyer">Buyer</option>
                        <option value="seller">Seller</option>
                        <option value="both">Both</option>
                    </select>
                </div>
                <div class="form-group">
                    <label>Password</label>
                    <input type="password" placeholder="Create password" required>
                </div>
                <button type="submit" class="btn btn-primary" style="width:100%;margin-top:10px;">Create Account</button>
            </form>
            <p style="text-align:center;margin-top:24px;font-size:14px;">
                Already have an account? <a href="#" onclick="switchToLogin()" style="color:var(--champagne);">Sign In</a>
            </p>
        </div>
    </div>

    <!-- OTP Modal -->
    <div class="modal-overlay" id="otpModal">
        <div class="modal">
            <button class="modal-close" onclick="closeModal('otpModal')">&times;</button>
            <h2 class="shimmer">Two-Factor Authentication</h2>
            <p>Enter the 6-digit code sent to your phone (+94)</p>
            <div class="otp-inputs">
                <input type="text" maxlength="1" class="otp-input" onkeyup="moveOTP(this, 0)">
                <input type="text" maxlength="1" class="otp-input" onkeyup="moveOTP(this, 1)">
                <input type="text" maxlength="1" class="otp-input" onkeyup="moveOTP(this, 2)">
                <input type="text" maxlength="1" class="otp-input" onkeyup="moveOTP(this, 3)">
                <input type="text" maxlength="1" class="otp-input" onkeyup="moveOTP(this, 4)">
                <input type="text" maxlength="1" class="otp-input" onkeyup="moveOTP(this, 5)">
            </div>
            <button class="btn btn-primary" style="width:100%;" onclick="verifyOTP()">Verify Code</button>
            <p style="text-align:center;margin-top:16px;font-size:13px;color:rgba(229,228,226,0.5);">
                Didn't receive? <a href="#" style="color:var(--champagne);" onclick="resendOTP()">Resend</a>
            </p>
        </div>
    </div>

    <!-- KYC Modal -->
    <div class="modal-overlay" id="kycModal">
        <div class="modal" style="max-width:600px;">
            <button class="modal-close" onclick="closeModal('kycModal')">&times;</button>
            <h2 class="shimmer">KYC Verification</h2>
            <p>Upload your documents to verify your identity</p>
            <form onsubmit="handleKYC(event)">
                <div class="form-group">
                    <label>NIC or Passport</label>
                    <div class="upload-zone" onclick="document.getElementById('nicUpload').click()">
                        <i class="fas fa-cloud-upload-alt"></i>
                        <p>Click to upload NIC or Passport</p>
                        <input type="file" id="nicUpload" style="display:none;" accept="image/*,.pdf">
                    </div>
                </div>
                <div class="form-group">
                    <label>Address Proof</label>
                    <div class="upload-zone" onclick="document.getElementById('addressUpload').click()">
                        <i class="fas fa-cloud-upload-alt"></i>
                        <p>Click to upload utility bill or bank statement</p>
                        <input type="file" id="addressUpload" style="display:none;" accept="image/*,.pdf">
                    </div>
                </div>
                <button type="submit" class="btn btn-primary" style="width:100%;margin-top:10px;">Submit for Verification</button>
            </form>
        </div>
    </div>

    <!-- Gem Viewer Modal -->
    <div class="modal-overlay viewer-modal" id="viewerModal">
        <div class="modal" style="max-width:900px;">
            <button class="modal-close" onclick="closeModal('viewerModal')">&times;</button>
            <div class="viewer-content">
                <div>
                    <div class="viewer-3d" id="viewer3d">
                        <canvas id="viewerCanvas" style="width:100%;height:100%;"></canvas>
                    </div>
                    <div class="viewer-controls">
                        <button onclick="rotateViewer('left')"><i class="fas fa-undo"></i> Rotate</button>
                        <button onclick="zoomViewer('in')"><i class="fas fa-search-plus"></i> Zoom In</button>
                        <button onclick="zoomViewer('out')"><i class="fas fa-search-minus"></i> Zoom Out</button>
                        <button onclick="resetViewer()"><i class="fas fa-sync"></i> Reset</button>
                    </div>
                    <div class="viewer-gallery">
                        <img src="https://images.unsplash.com/photo-1615655406736-b37c4fabf923?w=200&h=200&fit=crop" class="active" onclick="changeViewerImage(this)">
                        <img src="https://images.unsplash.com/photo-1599707367072-cd6ada2bc375?w=200&h=200&fit=crop" onclick="changeViewerImage(this)">
                        <img src="https://images.unsplash.com/photo-1605100804763-247f67b3557e?w=200&h=200&fit=crop" onclick="changeViewerImage(this)">
                        <img src="https://images.unsplash.com/photo-1617038260897-41a1f14a8ca0?w=200&h=200&fit=crop" onclick="changeViewerImage(this)">
                    </div>
                </div>
                <div class="viewer-info">
                    <h3 id="viewerGemName">Ceylon Blue Sapphire</h3>
                    <div class="price" id="viewerGemPrice">$12,500</div>
                    <div class="blockchain-badge">
                        <i class="fas fa-link"></i> Blockchain Verified
                    </div>
                    <div class="anti-theft-warning">
                        <i class="fas fa-exclamation-triangle"></i>
                        <span>Protected content. Right-click disabled. Screenshot detection active.</span>
                    </div>
                    <div class="details">
                        <p><span>Carat Weight</span> <span id="viewerWeight">5.2 ct</span></p>
                        <p><span>Color</span> <span id="viewerColor">Royal Blue</span></p>
                        <p><span>Clarity</span> <span id="viewerClarity">VVS1</span></p>
                        <p><span>Origin</span> <span id="viewerOrigin">Ratnapura, Sri Lanka</span></p>
                        <p><span>Certificate</span> <span id="viewerCert">GIA-2026-88421</span></p>
                        <p><span>Seller</span> <span id="viewerSeller">Priya Fernando</span></p>
                    </div>
                    <div style="display:flex;gap:12px;margin-top:24px;">
                        <button class="btn btn-primary" style="flex:1;" onclick="addToCartFromViewer()">
                            <i class="fas fa-shopping-cart"></i> Add to Cart
                        </button>
                        <button class="btn" onclick="scheduleVideoCall()">
                            <i class="fas fa-video"></i> Video Call
                        </button>
                    </div>
                    <button class="btn" style="width:100%;margin-top:12px;" onclick="downloadCertificate()">
                        <i class="fas fa-file-pdf"></i> Download Certificate
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Video Call Modal -->
    <div class="modal-overlay" id="videoModal">
        <div class="modal">
            <button class="modal-close" onclick="closeModal('videoModal')">&times;</button>
            <h2 class="shimmer">Schedule Video Inspection</h2>
            <p>Book a live video call to inspect this gemstone</p>
            <form onsubmit="scheduleVideo(event)">
                <div class="form-group">
                    <label>Platform</label>
                    <select required>
                        <option value="">Select platform</option>
                        <option value="zoom">Zoom</option>
                        <option value="google">Google Meet</option>
                    </select>
                </div>
                <div class="form-group">
                    <label>Preferred Date</label>
                    <input type="date" required>
                </div>
                <div class="form-group">
                    <label>Preferred Time</label>
                    <input type="time" required>
                </div>
                <div class="form-group">
                    <label>Timezone</label>
                    <select required>
                        <option value="">Select timezone</option>
                        <option value="UTC+5:30">Sri Lanka (UTC+5:30)</option>
                        <option value="UTC-5">EST (UTC-5)</option>
                        <option value="UTC+0">GMT (UTC+0)</option>
                        <option value="UTC+9">JST (UTC+9)</option>
                    </select>
                </div>
                <button type="submit" class="btn btn-primary" style="width:100%;margin-top:10px;">Schedule Call</button>
            </form>
        </div>
    </div>

    <!-- Calculator Modal -->
    <div class="modal-overlay" id="calcModal">
        <div class="modal">
            <button class="modal-close" onclick="closeModal('calcModal')">&times;</button>
            <h2 class="shimmer">Gem Price Calculator</h2>
            <p>Estimate the value of your gemstone</p>
            <form onsubmit="calculatePrice(event)">
                <div class="form-group">
                    <label>Gem Type</label>
                    <select id="calcType" required>
                        <option value="">Select type</option>
                        <option value="sapphire">Sapphire</option>
                        <option value="ruby">Ruby</option>
                        <option value="emerald">Emerald</option>
                        <option value="topaz">Topaz</option>
                        <option value="cats-eye">Cat's Eye</option>
                        <option value="garnet">Garnet</option>
                        <option value="aquamarine">Aquamarine</option>
                        <option value="alexandrite">Alexandrite</option>
                        <option value="moonstone">Moonstone</option>
                    </select>
                </div>
                <div class="form-group">
                    <label>Weight (Carats)</label>
                    <input type="number" id="calcWeight" step="0.01" placeholder="e.g. 2.5" required>
                </div>
                <div class="form-group">
                    <label>Clarity</label>
                    <select id="calcClarity" required>
                        <option value="">Select clarity</option>
                        <option value="fl">FL - Flawless</option>
                        <option value="if">IF - Internally Flawless</option>
                        <option value="vvs1">VVS1</option>
                        <option value="vvs2">VVS2</option>
                        <option value="vs1">VS1</option>
                        <option value="vs2">VS2</option>
                        <option value="si1">SI1</option>
                        <option value="si2">SI2</option>
                    </select>
                </div>
                <div class="form-group">
                    <label>Origin</label>
                    <select id="calcOrigin" required>
                        <option value="">Select origin</option>
                        <option value="ceylon">Sri Lanka (Ceylon)</option>
                        <option value="burma">Burma (Myanmar)</option>
                        <option value="thailand">Thailand</option>
                        <option value="africa">Africa</option>
                        <option value="brazil">Brazil</option>
                        <option value="other">Other</option>
                    </select>
                </div>
                <button type="submit" class="btn btn-primary" style="width:100%;margin-top:10px;">Calculate Price</button>
            </form>
            <div class="calc-result" id="calcResult" style="display:none;">
                <p style="font-size:14px;color:rgba(229,228,226,0.6);margin-bottom:8px;">Estimated Value</p>
                <div class="price" id="calcPrice">$0</div>
            </div>
        </div>
    </div>

    <!-- Checkout Modal -->
    <div class="modal-overlay" id="checkoutModal">
        <div class="modal" style="max-width:600px;">
            <button class="modal-close" onclick="closeModal('checkoutModal')">&times;</button>
            <h2 class="shimmer">Secure Checkout</h2>
            <p>Complete your purchase with buyer protection</p>
            <form onsubmit="handleCheckout(event)">
                <div style="margin-bottom:24px;padding:20px;border:1px solid var(--glass-border);">
                    <h4 style="font-size:18px;margin-bottom:16px;">Order Summary</h4>
                    <div id="checkoutItems"></div>
                    <div style="border-top:1px solid var(--glass-border);margin-top:16px;padding-top:16px;">
                        <div style="display:flex;justify-content:space-between;margin-bottom:8px;">
                            <span style="color:rgba(229,228,226,0.6);">Subtotal</span>
                            <span id="checkoutSubtotal">$0</span>
                        </div>
                        <div style="display:flex;justify-content:space-between;margin-bottom:8px;">
                            <span style="color:rgba(229,228,226,0.6);">Shipping</span>
                            <span id="checkoutShipping">$0</span>
                        </div>
                        <div style="display:flex;justify-content:space-between;margin-bottom:8px;">
                            <span style="color:rgba(229,228,226,0.6);">Tax</span>
                            <span id="checkoutTax">$0</span>
                        </div>
                        <div style="display:flex;justify-content:space-between;margin-top:16px;padding-top:16px;border-top:1px solid var(--glass-border);">
                            <span style="font-size:18px;">Total</span>
                            <span style="font-size:24px;color:var(--champagne);font-family:'Cormorant Garamond',serif;" id="checkoutTotal">$0</span>
                        </div>
                    </div>
                </div>
                <div class="form-group">
                    <label>Coupon Code</label>
                    <div style="display:flex;gap:8px;">
                        <input type="text" placeholder="Enter code" id="couponInput">
                        <button type="button" class="btn" onclick="applyCoupon()">Apply</button>
                    </div>
                </div>
                <div class="form-group">
                    <label>Shipping Address</label>
                    <textarea rows="3" placeholder="Full address..." required></textarea>
                </div>
                <div class="form-group">
                    <label>Payment Method</label>
                    <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;">
                        <button type="button" class="social-btn" onclick="selectPayment('visa')"><i class="fab fa-cc-visa"></i> Visa</button>
                        <button type="button" class="social-btn" onclick="selectPayment('mastercard')"><i class="fab fa-cc-mastercard"></i> Mastercard</button>
                        <button type="button" class="social-btn" onclick="selectPayment('amex')"><i class="fab fa-cc-amex"></i> Amex</button>
                        <button type="button" class="social-btn" onclick="selectPayment('stripe')"><i class="fab fa-stripe"></i> Stripe</button>
                        <button type="button" class="social-btn" onclick="selectPayment('paypal')"><i class="fab fa-paypal"></i> PayPal</button>
                        <button type="button" class="social-btn" onclick="selectPayment('apple')"><i class="fab fa-apple-pay"></i> Apple Pay</button>
                    </div>
                </div>
                <div style="padding:16px;background:rgba(34,197,94,0.05);border:1px solid rgba(34,197,94,0.3);margin-bottom:20px;">
                    <p style="font-size:13px;color:#4ade80;">
                        <i class="fas fa-shield-alt"></i> Buyer Protection: Full refund if item not as described. 30-day return policy.
                    </p>
                </div>
                <button type="submit" class="btn btn-primary" style="width:100%;">
                    <i class="fas fa-lock"></i> Place Secure Order
                </button>
            </form>
        </div>
    </div>

    <!-- Cart Sidebar -->
    <div class="cart-sidebar" id="cartSidebar">
        <div class="cart-header">
            <h3>Shopping Cart</h3>
            <button class="cart-close" onclick="closeCart()"><i class="fas fa-times"></i></button>
        </div>
        <div class="cart-items" id="cartItems"></div>
        <div class="cart-footer">
            <div class="cart-total">
                <span>Total</span>
                <span id="cartTotal">$0</span>
            </div>
            <button class="btn btn-primary" style="width:100%;margin-bottom:12px;" onclick="openCheckout()">Checkout</button>
            <button class="btn" style="width:100%;" onclick="clearCart()">Clear Cart</button>
        </div>
    </div>

    <!-- Live Chat Widget -->
    <div class="chat-widget">
        <div class="chat-window" id="chatWindow">
            <div class="chat-header">
                <h4>Live Support</h4>
                <button style="background:none;border:none;color:var(--platinum);cursor:pointer;" onclick="toggleChat()"><i class="fas fa-times"></i></button>
            </div>
            <div class="chat-messages" id="chatMessages">
                <div class="chat-message">
                    <p>Hello! Welcome to FITGEM. How can I assist you with your gemstone purchase today?</p>
                </div>
            </div>
            <div class="chat-input">
                <input type="text" placeholder="Type a message..." id="chatInput" onkeypress="handleChatKey(event)">
                <button onclick="sendChatMessage()"><i class="fas fa-paper-plane"></i></button>
            </div>
        </div>
        <button class="chat-toggle" onclick="toggleChat()"><i class="fas fa-comments"></i></button>
    </div>

    <!-- Screenshot Warning -->
    <div class="screenshot-warning" id="screenshotWarning">
        <i class="fas fa-ban"></i>
        <h2>Content Protection</h2>
        <p>Screenshots and screen recordings are prohibited on FITGEM. This content is protected by digital rights management. Violators will be banned and legal action may be taken.</p>
        <button class="btn" style="margin-top:24px;" onclick="closeScreenshotWarning()">I Understand</button>
    </div>

    <!-- PWA Install Button -->
    <button class="pwa-btn" id="pwaBtn" onclick="installPWA()">
        <i class="fas fa-download"></i> Install FITGEM App
    </button>

    <script>
        const gems = [
            { id: 1, name: "Royal Blue Sapphire", type: "Sapphire", price: 12500, carat: 5.2, color: "Royal Blue", clarity: "VVS1", origin: "Ratnapura, Sri Lanka", image: "https://images.unsplash.com/photo-1615655406736-b37c4fabf923?w=600&h=400&fit=crop", ai: true, rating: 4.9 },
            { id: 2, name: "Pigeon Blood Ruby", type: "Ruby", price: 8900, carat: 3.1, color: "Pigeon Blood Red", clarity: "VS1", origin: "Mogok, Burma", image: "https://images.unsplash.com/photo-1599707367072-cd6ada2bc375?w=600&h=400&fit=crop", ai: false, rating: 4.8 },
            { id: 3, name: "Colombian Emerald", type: "Emerald", price: 15600, carat: 4.8, color: "Vivid Green", clarity: "VVS2", origin: "Muzo, Colombia", image: "https://images.unsplash.com/photo-1605100804763-247f67b3557e?w=600&h=400&fit=crop", ai: true, rating: 4.9 },
            { id: 4, name: "Imperial Topaz", type: "Topaz", price: 3200, carat: 6.5, color: "Golden Imperial", clarity: "IF", origin: "Ouro Preto, Brazil", image: "https://images.unsplash.com/photo-1617038260897-41a1f14a8ca0?w=600&h=400&fit=crop", ai: false, rating: 4.7 },
            { id: 5, name: "Chrysoberyl Cat's Eye", type: "Cat's Eye", price: 7800, carat: 4.2, color: "Honey Green", clarity: "VS2", origin: "Ratnapura, Sri Lanka", image: "https://images.unsplash.com/photo-1535632066927-ab7c9ab60908?w=600&h=400&fit=crop", ai: false, rating: 4.8 },
            { id: 6, name: "Rhodolite Garnet", type: "Garnet", price: 2100, carat: 7.1, color: "Raspberry Red", clarity: "VS1", origin: "Sri Lanka", image: "https://images.unsplash.com/photo-1601121141461-9d6647bca1ed?w=600&h=400&fit=crop", ai: true, rating: 4.6 },
            { id: 7, name: "Santa Maria Aquamarine", type: "Aquamarine", price: 4500, carat: 5.8, color: "Deep Blue", clarity: "VVS1", origin: "Brazil", image: "https://images.unsplash.com/photo-1515562141127-7a88fb1971c7?w=600&h=400&fit=crop", ai: false, rating: 4.7 },
            { id: 8, name: "Russian Alexandrite", type: "Alexandrite", price: 28000, carat: 2.3, color: "Color Change", clarity: "VVS2", origin: "Ural Mountains, Russia", image: "https://images.unsplash.com/photo-1603561591411-07134e71a2a9?w=600&h=400&fit=crop", ai: true, rating: 5.0 },
            { id: 9, name: "Blue Moonstone", type: "Moonstone", price: 1200, carat: 8.5, color: "Blue Sheen", clarity: "VS2", origin: "Sri Lanka", image: "https://images.unsplash.com/photo-1608042314453-ae338d80c427?w=600&h=400&fit=crop", ai: false, rating: 4.5 },
            { id: 10, name: "Padparadscha Sapphire", type: "Sapphire", price: 18500, carat: 3.8, color: "Pink-Orange", clarity: "VVS1", origin: "Sri Lanka", image: "https://images.unsplash.com/photo-1615655406736-b37c4fabf923?w=600&h=400&fit=crop", ai: true, rating: 4.9 },
            { id: 11, name: "Star Ruby", type: "Ruby", price: 6500, carat: 6.2, color: "Deep Red", clarity: "VS2", origin: "Sri Lanka", image: "https://images.unsplash.com/photo-1599707367072-cd6ada2bc375?w=600&h=400&fit=crop", ai: false, rating: 4.7 },
            { id: 12, name: "Tsavorite Garnet", type: "Garnet", price: 3800, carat: 4.5, color: "Vivid Green", clarity: "VS1", origin: "Kenya", image: "https://images.unsplash.com/photo-1601121141461-9d6647bca1ed?w=600&h=400&fit=crop", ai: true, rating: 4.8 }
        ];

        let cart = [];
        let wishlist = [];
        let currentUser = null;

        document.addEventListener('DOMContentLoaded', function() {
            renderGems(gems);
            initParticles();
            init3DGem();
            initScrollAnimations();
            initNavbarScroll();
            initAntiTheft();
            initMusic(); // Auto-play music on page load
            setTimeout(() => { document.getElementById('pwaBtn').classList.add('visible'); }, 5000);
        });

        function renderGems(gemsToRender) {
            const grid = document.getElementById('gemsGrid');
            grid.innerHTML = gemsToRender.map(gem => `
                <div class="gem-card" onclick="openGemViewer(${gem.id})">
                    ${gem.ai ? '<div class="ai-badge"><i class="fas fa-robot"></i> AI Pick</div>' : ''}
                    <button class="wishlist-btn ${wishlist.includes(gem.id) ? 'active' : ''}" onclick="event.stopPropagation();toggleWishlist(${gem.id})">
                        <i class="fas fa-heart"></i>
                    </button>
                    <img src="${gem.image}" alt="${gem.name}" class="gem-image">
                    <div class="gem-info">
                        <div class="gem-type">${gem.type}</div>
                        <div class="gem-name">${gem.name}</div>
                        <div class="gem-price">$${gem.price.toLocaleString()}</div>
                        <div class="gem-origin"><i class="fas fa-map-marker-alt"></i> ${gem.origin}</div>
                        <div style="margin-top:12px;display:flex;align-items:center;gap:8px;">
                            <span style="color:var(--champagne);"><i class="fas fa-star"></i> ${gem.rating}</span>
                            <span style="font-size:12px;color:rgba(229,228,226,0.5);">${gem.carat} ct</span>
                        </div>
                    </div>
                </div>
            `).join('');
        }

        function initParticles() {
            const canvas = document.getElementById('particle-canvas');
            const ctx = canvas.getContext('2d');
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
            const particles = [];
            for (let i = 0; i < 50; i++) {
                particles.push({ x: Math.random() * canvas.width, y: Math.random() * canvas.height, size: Math.random() * 2 + 0.5, speedX: (Math.random() - 0.5) * 0.5, speedY: (Math.random() - 0.5) * 0.5, opacity: Math.random() * 0.5 + 0.1 });
            }
            function animate() {
                ctx.clearRect(0, 0, canvas.width, canvas.height);
                particles.forEach(p => {
                    p.x += p.speedX; p.y += p.speedY;
                    if (p.x < 0) p.x = canvas.width; if (p.x > canvas.width) p.x = 0;
                    if (p.y < 0) p.y = canvas.height; if (p.y > canvas.height) p.y = 0;
                    ctx.beginPath(); ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
                    ctx.fillStyle = `rgba(247, 231, 206, ${p.opacity})`; ctx.fill();
                });
                requestAnimationFrame(animate);
            }
            animate();
            window.addEventListener('resize', () => { canvas.width = window.innerWidth; canvas.height = window.innerHeight; });
        }

        function init3DGem() {
            const container = document.getElementById('gem-canvas');
            if (!container) return;
            const scene = new THREE.Scene();
            const camera = new THREE.PerspectiveCamera(75, container.clientWidth / container.clientHeight, 0.1, 1000);
            const renderer = new THREE.WebGLRenderer({ alpha: true, antialias: true });
            renderer.setSize(container.clientWidth, container.clientHeight);
            renderer.setPixelRatio(window.devicePixelRatio);
            container.appendChild(renderer.domElement);
            const geometry = new THREE.OctahedronGeometry(2, 0);
            const material = new THREE.MeshPhysicalMaterial({ color: 0x4169E1, metalness: 0.1, roughness: 0.1, transmission: 0.6, thickness: 1, clearcoat: 1, clearcoatRoughness: 0.1, envMapIntensity: 1 });
            const gem = new THREE.Mesh(geometry, material);
            scene.add(gem);
            const ambientLight = new THREE.AmbientLight(0xffffff, 0.5);
            scene.add(ambientLight);
            const pointLight1 = new THREE.PointLight(0xF7E7CE, 1, 100);
            pointLight1.position.set(5, 5, 5); scene.add(pointLight1);
            const pointLight2 = new THREE.PointLight(0xB76E79, 0.8, 100);
            pointLight2.position.set(-5, -5, 5); scene.add(pointLight2);
            camera.position.z = 5;
            function animate() {
                requestAnimationFrame(animate);
                gem.rotation.x += 0.005; gem.rotation.y += 0.01;
                renderer.render(scene, camera);
            }
            animate();
            window.addEventListener('resize', () => {
                camera.aspect = container.clientWidth / container.clientHeight;
                camera.updateProjectionMatrix();
                renderer.setSize(container.clientWidth, container.clientHeight);
            });
        }

        function initScrollAnimations() {
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('visible');
                        if (entry.target.querySelector('.stat-number')) animateCounters();
                    }
                });
            }, { threshold: 0.1 });
            document.querySelectorAll('.fade-in').forEach(el => observer.observe(el));
        }

        function animateCounters() {
            document.querySelectorAll('.stat-number').forEach(counter => {
                const target = parseInt(counter.getAttribute('data-target'));
                const duration = 2000; const start = 0; const startTime = performance.now();
                function update(currentTime) {
                    const elapsed = currentTime - startTime;
                    const progress = Math.min(elapsed / duration, 1);
                    const easeOut = 1 - Math.pow(1 - progress, 3);
                    const current = Math.floor(start + (target - start) * easeOut);
                    counter.textContent = current.toLocaleString() + (target === 99 ? '%' : '+');
                    if (progress < 1) requestAnimationFrame(update);
                }
                requestAnimationFrame(update);
            });
        }

        function initNavbarScroll() {
            window.addEventListener('scroll', () => {
                const navbar = document.getElementById('navbar');
                if (window.scrollY > 50) navbar.classList.add('scrolled');
                else navbar.classList.remove('scrolled');
            });
        }

        function initAntiTheft() {
            document.addEventListener('contextmenu', (e) => {
                if (e.target.closest('.gem-card') || e.target.closest('.viewer-modal')) { e.preventDefault(); showScreenshotWarning(); }
            });
            document.addEventListener('dragstart', (e) => { if (e.target.tagName === 'IMG') e.preventDefault(); });
            document.addEventListener('keydown', (e) => { if (e.key === 'PrintScreen' || (e.ctrlKey && e.key === 'p')) showScreenshotWarning(); });
        }

        function showScreenshotWarning() { document.getElementById('screenshotWarning').classList.add('active'); }
        function closeScreenshotWarning() { document.getElementById('screenshotWarning').classList.remove('active'); }
        function openModal(id) { document.getElementById(id).classList.add('active'); document.body.style.overflow = 'hidden'; }
        function closeModal(id) { document.getElementById(id).classList.remove('active'); document.body.style.overflow = ''; }
        function openLogin() { openModal('loginModal'); }
        function openRegister() { openModal('registerModal'); }
        function switchToRegister() { closeModal('loginModal'); setTimeout(() => openModal('registerModal'), 300); }
        function switchToLogin() { closeModal('registerModal'); setTimeout(() => openModal('loginModal'), 300); }
        function handleLogin(e) { e.preventDefault(); closeModal('loginModal'); setTimeout(() => openModal('otpModal'), 300); }
        function handleRegister(e) { e.preventDefault(); closeModal('registerModal'); setTimeout(() => openModal('otpModal'), 300); }
        function moveOTP(input, index) { if (input.value && index < 5) document.querySelectorAll('.otp-input')[index + 1].focus(); }
        function verifyOTP() { closeModal('otpModal'); currentUser = { name: 'Menuka K.', email: 'menuka@fitgem.lk' }; document.getElementById('loginBtn').style.display = 'none'; document.getElementById('userDropdown').style.display = 'block'; document.getElementById('userName').textContent = currentUser.name; showNotification('Successfully logged in!'); }
        function resendOTP() { showNotification('New OTP sent to your phone (+94)'); }
        function socialLogin(provider) { showNotification(`Connecting to ${provider}...`); setTimeout(() => { closeModal('loginModal'); closeModal('registerModal'); setTimeout(() => openModal('otpModal'), 300); }, 1000); }
        function toggleUserMenu() { document.getElementById('userMenu').classList.toggle('active'); }
        function showProfile() { document.getElementById('userMenu').classList.remove('active'); showNotification('Profile page opened'); }
        function showOrders() { document.getElementById('userMenu').classList.remove('active'); showNotification('Orders page opened'); }
        function showWishlist() { document.getElementById('userMenu').classList.remove('active'); showNotification(`Wishlist: ${wishlist.length} items`); }
        function showKYC() { document.getElementById('userMenu').classList.remove('active'); openModal('kycModal'); }
        function logout() { document.getElementById('userMenu').classList.remove('active'); currentUser = null; document.getElementById('loginBtn').style.display = 'block'; document.getElementById('userDropdown').style.display = 'none'; showNotification('Logged out successfully'); }
        function handleKYC(e) { e.preventDefault(); closeModal('kycModal'); showNotification('KYC documents submitted for verification'); }
        function openCart() { document.getElementById('cartSidebar').classList.add('active'); }
        function closeCart() { document.getElementById('cartSidebar').classList.remove('active'); }
        function addToCart(gemId) { const gem = gems.find(g => g.id === gemId); if (gem && !cart.find(c => c.id === gemId)) { cart.push(gem); updateCartUI(); showNotification(`${gem.name} added to cart`); } }
        function addToCartFromViewer() { const gemId = parseInt(document.getElementById('viewerGemName').dataset.id); addToCart(gemId); closeModal('viewerModal'); }
        function removeFromCart(gemId) { cart = cart.filter(c => c.id !== gemId); updateCartUI(); }
        function clearCart() { cart = []; updateCartUI(); }
        function updateCartUI() {
            document.getElementById('cartCount').textContent = cart.length;
            const cartItems = document.getElementById('cartItems');
            if (cart.length === 0) cartItems.innerHTML = '<p style="text-align:center;color:rgba(229,228,226,0.5);padding:40px;">Your cart is empty</p>';
            else cartItems.innerHTML = cart.map(gem => `<div class="cart-item"><img src="${gem.image}" alt="${gem.name}"><div class="cart-item-info"><h4>${gem.name}</h4><p>$${gem.price.toLocaleString()}</p><div class="cart-item-actions"><button onclick="removeFromCart(${gem.id})">Remove</button><button onclick="saveForLater(${gem.id})">Save for Later</button></div></div></div>`).join('');
            const total = cart.reduce((sum, g) => sum + g.price, 0);
            document.getElementById('cartTotal').textContent = '$' + total.toLocaleString();
        }
        function saveForLater(gemId) { showNotification('Item saved for later'); }
        function openCheckout() {
            closeCart();
            const checkoutItems = document.getElementById('checkoutItems');
            const subtotal = cart.reduce((sum, g) => sum + g.price, 0);
            const shipping = subtotal > 10000 ? 0 : 150;
            const tax = subtotal * 0.08;
            const total = subtotal + shipping + tax;
            checkoutItems.innerHTML = cart.map(gem => `<div style="display:flex;justify-content:space-between;margin-bottom:12px;"><span>${gem.name} (${gem.carat}ct)</span><span>$${gem.price.toLocaleString()}</span></div>`).join('');
            document.getElementById('checkoutSubtotal').textContent = '$' + subtotal.toLocaleString();
            document.getElementById('checkoutShipping').textContent = shipping === 0 ? 'FREE' : '$' + shipping;
            document.getElementById('checkoutTax').textContent = '$' + tax.toLocaleString(undefined, {maximumFractionDigits: 0});
            document.getElementById('checkoutTotal').textContent = '$' + total.toLocaleString(undefined, {maximumFractionDigits: 0});
            openModal('checkoutModal');
        }
        function applyCoupon() { const code = document.getElementById('couponInput').value; if (code === 'FITGEM20') showNotification('Coupon applied! 20% discount'); else showNotification('Invalid coupon code'); }
        function selectPayment(method) { showNotification(`${method.toUpperCase()} selected`); }
        function handleCheckout(e) { e.preventDefault(); closeModal('checkoutModal'); cart = []; updateCartUI(); showNotification('Order placed successfully! Order tracking enabled.'); }
        function toggleWishlist(gemId) { const index = wishlist.indexOf(gemId); if (index > -1) { wishlist.splice(index, 1); showNotification('Removed from wishlist'); } else { wishlist.push(gemId); showNotification('Added to wishlist'); } renderGems(gems); }
        function openGemViewer(gemId) {
            const gem = gems.find(g => g.id === gemId); if (!gem) return;
            document.getElementById('viewerGemName').textContent = gem.name;
            document.getElementById('viewerGemName').dataset.id = gem.id;
            document.getElementById('viewerGemPrice').textContent = '$' + gem.price.toLocaleString();
            document.getElementById('viewerWeight').textContent = gem.carat + ' ct';
            document.getElementById('viewerColor').textContent = gem.color;
            document.getElementById('viewerClarity').textContent = gem.clarity;
            document.getElementById('viewerOrigin').textContent = gem.origin;
            openModal('viewerModal');
            initViewer3D();
        }
        function initViewer3D() { const canvas = document.getElementById('viewerCanvas'); let rotation = 0; function rotate() { rotation += 0.01; canvas.style.transform = `rotateY(${rotation}rad)`; requestAnimationFrame(rotate); } rotate(); }
        function rotateViewer(direction) { showNotification('Rotating gem...'); }
        function zoomViewer(direction) { showNotification(`Zoom ${direction}`); }
        function resetViewer() { showNotification('View reset'); }
        function changeViewerImage(img) { document.querySelectorAll('.viewer-gallery img').forEach(i => i.classList.remove('active')); img.classList.add('active'); }
        function downloadCertificate() { showNotification('Certificate download started...'); }
        function scheduleVideoCall() { openModal('videoModal'); }
        function scheduleVideo(e) { e.preventDefault(); closeModal('videoModal'); showNotification('Video call scheduled! Check your email for the link.'); }
        function openCalculator() { openModal('calcModal'); }
        function calculatePrice(e) {
            e.preventDefault();
            const type = document.getElementById('calcType').value;
            const weight = parseFloat(document.getElementById('calcWeight').value);
            const clarity = document.getElementById('calcClarity').value;
            const origin = document.getElementById('calcOrigin').value;
            const basePrices = { sapphire: 2000, ruby: 2500, emerald: 3000, topaz: 400, 'cats-eye': 1500, garnet: 300, aquamarine: 600, alexandrite: 10000, moonstone: 150 };
            const clarityMult = { fl: 3.0, if: 2.5, vvs1: 2.0, vvs2: 1.8, vs1: 1.5, vs2: 1.3, si1: 1.0, si2: 0.7 };
            const originMult = { ceylon: 1.5, burma: 1.8, thailand: 1.2, africa: 1.0, brazil: 1.1, other: 0.9 };
            const base = basePrices[type] || 500;
            const price = base * weight * (clarityMult[clarity] || 1) * (originMult[origin] || 1);
            document.getElementById('calcPrice').textContent = '$' + Math.round(price).toLocaleString();
            document.getElementById('calcResult').style.display = 'block';
        }
        function searchGems() {
            const query = document.getElementById('searchInput').value.toLowerCase();
            const filtered = gems.filter(g => g.name.toLowerCase().includes(query) || g.type.toLowerCase().includes(query) || g.origin.toLowerCase().includes(query) || g.color.toLowerCase().includes(query));
            renderGems(filtered);
            document.getElementById('gems').scrollIntoView({ behavior: 'smooth' });
        }
        function filterCategory(category) { const filtered = gems.filter(g => g.type === category); renderGems(filtered); document.getElementById('gems').scrollIntoView({ behavior: 'smooth' }); }
        function scrollToGems() { document.getElementById('gems').scrollIntoView({ behavior: 'smooth' }); }
        function toggleLangMenu() { document.getElementById('langMenu').classList.toggle('active'); }
        function toggleToneMenu() { document.getElementById('toneMenu').classList.toggle('active'); }
        function changeLang(lang) { document.getElementById('langMenu').classList.remove('active'); showNotification(`Language changed to ${lang}`); }
        function toggleMobileMenu() { document.getElementById('mobileMenu').classList.toggle('active'); }
        function toggleChat() { document.getElementById('chatWindow').classList.toggle('active'); }
        function sendChatMessage() {
            const input = document.getElementById('chatInput');
            const message = input.value.trim(); if (!message) return;
            const messages = document.getElementById('chatMessages');
            messages.innerHTML += `<div class="chat-message sent"><p>${message}</p></div>`;
            input.value = ''; messages.scrollTop = messages.scrollHeight;
            setTimeout(() => { messages.innerHTML += `<div class="chat-message"><p>Thank you for your message! Our gem specialist will assist you shortly. Would you like to schedule a video call to inspect any gemstone?</p></div>`; messages.scrollTop = messages.scrollHeight; }, 1500);
        }
        function handleChatKey(e) { if (e.key === 'Enter') sendChatMessage(); }
        function showHome() { document.getElementById('mainContent').style.display = 'block'; document.getElementById('sellerDashboard').classList.remove('active'); document.getElementById('adminPanel').classList.remove('active'); window.scrollTo(0, 0); }
        function showAdminPanel() { document.getElementById('mainContent').style.display = 'none'; document.getElementById('sellerDashboard').classList.remove('active'); document.getElementById('adminPanel').classList.add('active'); window.scrollTo(0, 0); initPaymentChart(); }
        function switchTab(tab) { document.querySelectorAll('.admin-tab').forEach(t => t.classList.remove('active')); event.target.classList.add('active'); document.querySelectorAll('.tab-content').forEach(c => c.style.display = 'none'); document.getElementById('tab-' + tab).style.display = 'block'; }
        function showSellerDashboard() { document.getElementById('mainContent').style.display = 'none'; document.getElementById('adminPanel').classList.remove('active'); document.getElementById('sellerDashboard').classList.add('active'); window.scrollTo(0, 0); initSalesChart(); }
        function openAddListing() { showNotification('Add listing form opened'); }
        function exportAnalytics() { showNotification('Analytics exported to CSV'); }
        function promoteListing() { showNotification('Listing promotion options opened'); }
        function initSalesChart() {
            const ctx = document.getElementById('salesChart'); if (!ctx) return;
            new Chart(ctx, { type: 'line', data: { labels: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'], datasets: [{ label: 'Sales ($)', data: [12000, 19000, 15000, 25000, 32000, 48250], borderColor: '#F7E7CE', backgroundColor: 'rgba(247, 231, 206, 0.1)', borderWidth: 2, tension: 0.4, fill: true }] }, options: { responsive: true, plugins: { legend: { display: false } }, scales: { x: { grid: { color: 'rgba(255,255,255,0.05)' }, ticks: { color: 'rgba(229,228,226,0.6)' } }, y: { grid: { color: 'rgba(255,255,255,0.05)' }, ticks: { color: 'rgba(229,228,226,0.6)' } } } } });
        }
        function initPaymentChart() {
            const ctx = document.getElementById('paymentChart'); if (!ctx) return;
            new Chart(ctx, { type: 'bar', data: { labels: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'], datasets: [{ label: 'Payment Volume ($)', data: [45000, 62000, 58000, 78000, 95000, 112000], backgroundColor: 'rgba(247, 231, 206, 0.3)', borderColor: '#F7E7CE', borderWidth: 1 }] }, options: { responsive: true, plugins: { legend: { display: false } }, scales: { x: { grid: { color: 'rgba(255,255,255,0.05)' }, ticks: { color: 'rgba(229,228,226,0.6)' } }, y: { grid: { color: 'rgba(255,255,255,0.05)' }, ticks: { color: 'rgba(229,228,226,0.6)' } } } } });
        }
        function installPWA() { showNotification('Installing FITGEM app...'); document.getElementById('pwaBtn').classList.remove('visible'); }
        function showNotification(message) {
            const notification = document.createElement('div');
            notification.style.cssText = 'position: fixed; top: 100px; right: 30px; padding: 16px 24px; background: rgba(5,5,5,0.95); border: 1px solid rgba(247,231,206,0.3); color: var(--champagne); font-size: 14px; z-index: 5000; animation: slideIn 0.3s ease;';
            notification.textContent = message;
            document.body.appendChild(notification);
            setTimeout(() => { notification.style.animation = 'slideOut 0.3s ease'; setTimeout(() => notification.remove(), 300); }, 3000);
        }
        document.querySelectorAll('.modal-overlay').forEach(overlay => {
            overlay.addEventListener('click', (e) => { if (e.target === overlay) { overlay.classList.remove('active'); document.body.style.overflow = ''; } });
        });
        const style = document.createElement('style');
        style.textContent = '@keyframes slideIn { from { transform: translateX(100px); opacity: 0; } to { transform: translateX(0); opacity: 1; } } @keyframes slideOut { from { transform: translateX(0); opacity: 1; } to { transform: translateX(100px); opacity: 0; } }';
        document.head.appendChild(style);

        // Music Auto-Play & Toggle
        let musicPlaying = false;
        let musicInitialized = false;

        function initMusic() {
            const audio = document.getElementById('bgMusic');
            audio.volume = 0.4;

            // Try to play immediately
            const playPromise = audio.play();

            if (playPromise !== undefined) {
                playPromise.then(() => {
                    musicPlaying = true;
                    musicInitialized = true;
                    document.getElementById('musicIcon').className = 'fas fa-volume-up';
                    console.log('Music auto-played successfully');
                }).catch(err => {
                    console.log('Auto-play blocked, waiting for user interaction...');
                    // Show visual cue that music is ready
                    document.getElementById('musicIcon').className = 'fas fa-music';

                    // Try on any user interaction
                    const events = ['click', 'touchstart', 'keydown', 'scroll', 'mousemove'];
                    events.forEach(evt => {
                        document.addEventListener(evt, tryAutoPlay, { once: true, passive: true });
                    });
                });
            }
        }

        function tryAutoPlay() {
            if (musicInitialized) return;
            const audio = document.getElementById('bgMusic');
            audio.volume = 0.4;
            audio.play().then(() => {
                musicPlaying = true;
                musicInitialized = true;
                document.getElementById('musicIcon').className = 'fas fa-volume-up';
                showNotification('Music started');
            }).catch(e => {
                console.log('Still blocked:', e);
            });
        }

        function toggleMusic() {
            const audio = document.getElementById('bgMusic');
            const icon = document.getElementById('musicIcon');
            if (!musicInitialized) {
                initMusic();
                return;
            }
            if (musicPlaying) {
                audio.pause();
                icon.className = 'fas fa-music';
                showNotification('Music paused');
            } else {
                audio.volume = 0.4;
                audio.play();
                icon.className = 'fas fa-volume-up';
                showNotification('Music playing');
            }
            musicPlaying = !musicPlaying;
        }

        // AI Tone Switcher
        let currentTone = 'luxury';
        function switchTone(tone) {
            currentTone = tone;
            const root = document.documentElement;
            if (tone === 'cram') {
                root.style.setProperty('--platinum', '#ffffff');
                root.style.setProperty('--champagne', '#ffd700');
                root.style.setProperty('--rose-gold', '#ff6b6b');
                showNotification('AI Cram tone activated');
            } else {
                root.style.setProperty('--platinum', '#E5E4E2');
                root.style.setProperty('--champagne', '#F7E7CE');
                root.style.setProperty('--rose-gold', '#B76E79');
                showNotification('Luxury tone restored');
            }
        }
    </script>

    <!-- Ambient Background Music -->
    <audio id="bgMusic" loop preload="auto">
        <source src="https://www.zedge.net/ringtones/29d88a19-814e-4fd6-9501-89795fb7b501" type="audio/mpeg">
    </audio>

</body>
</html>